# Required fonts

Place these two font files in this directory before validating or installing the
configuration:

- `ocraext.ttf`
- `materialdesignicons-webfont.ttf`

The font files are not included because their redistribution terms may differ
from the license for this project. Keep the filenames exactly as shown.
